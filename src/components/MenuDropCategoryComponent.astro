---
import { MenuDropCategory } from "../data/ItemsMenu"
---

<!--mapeo  -->
<div class="grid grid-cols-3 gap-5 ">
    { MenuDropCategory.map((item)=>
        <a href={item.href} class="bg-red-200 rounded-xl text-center text-black px-4 py-2 text-lg font-semibold hover:bg-red-400 block">{item.texto}</a>
    )
        }
        
</div>

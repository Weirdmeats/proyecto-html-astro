---
import { MenuDropCategory } from "../data/ItemsMenu"
---

<!--mapeo  -->
<div class="grid grid-cols-3 gap-5 ">
    <!--mapeo de los productos -->
    { MenuDropCategory.map((item)=>
        <a href={item.href} class="bg-azul-medio rounded-xl text-center text-blanco px-4 py-2 text-lg font-semibold hover:bg-slate-400 block">{item.texto}</a>
    )
        }
        
</div>
